# Enable Gradle toolchain auto-download so builds run on environments without JDK 17 installed
org.gradle.java.installations.auto-download=true
# Use Maven Central as the repository for toolchain downloads
org.gradle.java.installations.auto-download.repositories=maven-central
# Target Java 21 for Railway deployment
java.version=21
